# SparkArc 剧本格式规范 (.arc)

## 概述
`.arc` 是 SparkArc Studio 的创作阶段剧本格式，专为 LLM 创作优化，兼顾人类可读性与程序可解析性。

---

## 格式元素

### 1. 场景标题 (Scene Title)
以 `#` 开头的一级标题，作为场景的唯一标识符（对应 `.story` 的 `scene` 字段）。

```
# 第一章：序幕
```

### 2. 章节/任务标题 (@cap)
以 `@cap` 开头，定义当前场景的任务名/章节名。

```
@cap 黑暗中的交易
```

### 3. 场景引言 (@intro)
以 `@intro` 开头，作为**每个场景的引言/前置说明**（例如：场景目的、氛围、信息铺垫、时间地点切换说明等）。

支持两种写法：

**单行写法：**
```
@intro 本场景将揭示“隐藏账本”的来源，并把氛围推向紧张。
```

**多行写法：**
```
@intro
本场景目标：找到加密账本。
氛围：霓虹、潮湿、压抑。
冲突：神秘人试探主角底线。
```

> 解析建议：`@intro` 后的内容直到遇到空行或下一个格式元素为止。

### 4. 思维链 (<thought>)
**仅供 AI 创作时使用，不会出现在最终剧本中。**

```xml
<thought>
[POV Check]: Current perspective is chr=0 (主角). Using "我" for internal thoughts.
[Scene Goal]: Player discovers the hidden safe.
[Atmosphere]: Tense, noir, dimly lit.
</thought>
```

### 5. 旁白 (Narration)
以 `(旁白)` 标记，后跟叙述文本。

```
(旁白)
空气中弥漫着纸张霉变的味道，混杂着老式散热器焦糊的气息。
```

### 6. 角色对话 (Dialogue)
使用数字 ID 标识角色，格式为 `[数字]`，后跟对话内容。
- 数字对应 `chr.bind` 中的角色 ID
- 括号内的文字为表情/动作提示

```
[0]
（轻按太阳穴）
这地方起码荒废了十年，真的会有我要找的东西吗？

[1]
……你终于来了。
```

### 7. 选项分支 (<choice>)
使用 XML 风格的 `<choice>` 和 `<opt>` 标签。

```xml
<choice>
  <opt text="选项A的显示文本">
    (旁白)
    选择A后的叙述...
    
    [0]
    选择A后的对话...
  </opt>
  
  <opt text="选项B的显示文本">
    [0]
    选择B后的对话...
  </opt>
</choice>
```

**支持多层嵌套：**
```xml
<choice>
  <opt text="外层选项">
    [0]
    对话...
    
    <choice>
      <opt text="内层选项1">
        ...
      </opt>
      <opt text="内层选项2">
        ...
      </opt>
    </choice>
  </opt>
</choice>
```

### 8. 场景跳转 (@next)
在对话末尾使用 `@next` 指定跳转到的场景名。

```
[0]
看来你的炸弹哑火了。现在，我们可以好好谈谈了吗？
@next 审讯开始
```

### 9. 行为指令 (@act) - 仅人类添加
**此元素不应由 AI 生成**，仅供人工在编辑器中添加。

```
[0]
准备好了吗？
@act bgm:战斗主题曲
@act weather:rainy,2h
```

---

## 角色 ID 映射

角色使用数字 ID，由 `chr.bind` 文件定义映射关系：

```json
{
  "0": "陈探长",
  "1": "神秘人",
  "2": "黑客",
  "3": "系统"
}
```

**对 AI 的提示**：创作时会提供角色 ID 与名称的映射表，使用 `[数字]` 格式标识角色。

---

## 与 .story JSON 的对应关系

| .arc 元素 | .story JSON 字段 |
|-----------|------------------|
| `# 标题` | `scene` |
| `@cap 名称` | `cap` |
| `@intro ...` | `intro` |
| `[数字]` | `chr` |
| 对话文本 | `txt` |
| `<choice>/<opt>` | `opt` / `optn` / `dia` |
| `@next 场景名` | `next` |
| `@act key:value` | `act` |

---

## AI 创作示例（给 Scriptwriter Agent）

**不包含 `@act` 指令，不包含 `<thought>` 在最终输出**

```
# 咖啡厅的邂逅
@cap 命运的交汇

(旁白)
午后的阳光透过落地窗洒进来，在木质地板上投下斑驳的光影。

[0]
（搅动着杯中的咖啡）
今天的拿铁比平时淡了一些。

[1]
（从门口走进）
这个位置有人吗？

<choice>
  <opt text="抬头微笑">
    [0]
    （放下杯子）
    请便。
    
    (旁白)
    我注意到她手中拿着一本泛黄的旧书，封面上的字迹已经模糊不清。
  </opt>
  
  <opt text="假装没听见">
    (旁白)
    我低下头，继续盯着手机屏幕，假装没有听到她的声音。
    
    [1]
    （轻笑）
    看来你不太擅长演戏呢。
  </opt>
</choice>
```

---

## 解析注意事项

1. **空行分隔**：元素之间用空行分隔
2. **缩进敏感**：`<choice>` 内的内容保持一致缩进
3. **多行对话**：角色 ID 后的连续非空行都属于该角色的对话
4. **转义**：文本中的 `<` `>` 如非标签，应保持原样（解析器智能识别）
